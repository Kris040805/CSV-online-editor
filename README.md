# CSV-online-editor

![CSV SQL Tool](https://img.shields.io/badge/Project-CSV%20SQL%20Tool-blue)

**CSV SQL Tool** е учебен Spring Boot проект, който предоставя REST API за работа със CSV файлове и изпълнение на SQL
заявки върху временна база данни (H2 in-memory). Проектът демонстрира основни принципи на backend архитектура, динамично
генериране на таблици и сигурност при изпълнение на SQL.

**Проектът все още е в развитие. Скоро ще има и frontend.**

---

## 📝 Функционалности

### 1️⃣ Импорт на CSV (`/api/import-csv`)

- Качване на CSV файл чрез POST заявка.
- Динамично:
    - Определяне на типовете на колоните (INT, DOUBLE, BOOLEAN, VARCHAR).
    - Създаване на временна таблица в H2.
    - Вмъкване на всички редове от CSV.

**Пример на response:**

```json
{
  "tableName": "temporary_table",
  "rows": 10,
  "columns": 4,
  "headers": [
    "ID",
    "Name",
    "Age",
    "Active"
  ]
}
```

### 2️⃣ Изпълнение на SQL заявки (`/api/execute-sql`)

- Приема SQL заявка като JSON поле query.
- Поддържа:
    - SELECT → връща редове и колони като JSON.
    - INSERT, UPDATE, DELETE → връща броя засегнати редове.
- Валидация:
    - Забранява DROP, TRUNCATE, ALTER.
    - Забранява DELETE без WHERE.
    - Проверява празни заявки.
- Логване на всяка изпълнена заявка.
- Разграничаване на клиентски и сървърни грешки (HTTP 400 vs 500).

**Пример на SELECT response:**
```json
{
  "query": "SELECT * FROM temporary_table",
  "type": "SELECT",
  "rowsCount": 3,
  "rows": [
    {
      "ID": 1,
      "Name": "Moni",
      "Age": 19,
      "Active": true
    },
    {
      "ID": 2,
      "Name": "Kris",
      "Age": 20,
      "Active": false
    }
  ]
}
```

### 3️⃣ Експорт на CSV (`/api/export-csv`)
- Връща съдържанието на таблица като CSV файл.
- Header редът се генерира автоматично.
- Стойностите се escape-ват правилно според CSV стандарт.
- Връща файл за сваляне с правилно зададено име.

**Пример на CSV output:**
```
ID,Name,Age,Active
1,"Moni",19,true
2,"Kris",20,false
3,"Vladi",35,true
```


### 4️⃣ Логване и наблюдение
- Всяка заявка се логва в Service слоя.
- Контролерът логва грешки и предупреждения.
- Разделяне на INFO / WARN / ERROR за лесно следене.

### 5️⃣ Сигурност и защита
- Ограничения върху опасни SQL операции.
- Права за изпълнение само чрез валидни заявки.
- Валидация на tableName при export.
- Обработка на грешки и ясни съобщения за клиента.

### ⚙️ Технологии
- Java 17 / Spring Boot 3
- Spring JDBC (JdbcTemplate)
- H2 in-memory база данни
- Apache Commons CSV (за CSV parsing)
- SLF4J / Logback за логване
- REST API (JSON)