# CSV-online-editor


![CSV SQL Tool](https://img.shields.io/badge/Project-CSV%20SQL%20Tool-blue)

**CSV SQL Tool** е учебен Spring Boot проект, който предоставя REST API за работа с CSV файлове и изпълнение на SQL заявки върху временна база данни (H2 in-memory). Проектът демонстрира основни принципи на backend архитектура, динамично генериране на таблици и сигурност при изпълнение на SQL.

---

## 📝 Функционалности

### 1️⃣ Импорт на CSV (`/api/import-csv`)
- Качване на CSV файл чрез POST заявка.
- Динамично:
    - Определяне на типовете колони (INT, DOUBLE, BOOLEAN, VARCHAR).
    - Създаване на временна таблица в H2.
    - Вмъкване на всички редове от CSV.
- Връща JSON с информация за:
    - Име на таблицата
    - Брой редове
    - Брой колони
    - Header-и и данни

**Пример на response:**
```json
{
  "tableName": "temporary_table",
  "rows": 10,
  "columns": 4,
  "headers": ["ID","Name","Age","Active"]
}
